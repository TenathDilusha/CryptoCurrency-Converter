# Example environment variables for CryptoCurrency Converter
# Copy this file to `.env` and fill in values before running the app.

# Exchange Rates API Key (get free key from https://exchangeratesapi.io/)
EXCHANGE_API_KEY=your_api_key_here

# Server port (default: 3000)
PORT=3000
